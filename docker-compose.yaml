services:
  playwright-runner:
    build: .
    environment:
      # This pulls from Jenkins, or defaults to your Windows host bridge
      - BASE_URL=${BASE_URL}
      
  jenkins:
    image: jenkins/jenkins:lts
    environment:
      - JAVA_OPTS=-Dhudson.model.DirectoryBrowserSupport.CSP=""